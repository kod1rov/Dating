<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.devcraft.ratingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RatingApp">
        <activity
            android:name=".ui.activity.ProfileEditActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.bnv.BNVActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.match.MatchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.fragments.chat.activity.DialogActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>